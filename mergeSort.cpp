#include <iostream>
using namespace std;

int arr[20], B[20]; // Array utama dan Array bantu
int n;



void mergeSort (int low, int high) {
    if (low >= high){ //step1
        return; // step 1a
    }

    int mid = (low + high) / 2; // step 2
}
